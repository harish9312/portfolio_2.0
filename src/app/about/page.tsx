import { BasePage } from "@/components/BasePage";
import { Title } from "@/components/Title";

const AboutPage = () => {
  return (
    <BasePage>
      <Title>About</Title>
    </BasePage>
  );
};

export default AboutPage;
